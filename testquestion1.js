let ball
function setup(){
 createCanvas(600, 400);
 background(220);
}
function draw() {
background(220)

background(220)
if (mouse.presses()) {
    ball = new Sprite(mouse.x,mouse.y,50)
    ball.mass = 10;
}
}
